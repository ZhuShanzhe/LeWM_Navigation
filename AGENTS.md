# Project instructions

This is the canonical repository for this user's LeWM × Navigation research:
https://github.com/ZhuShanzhe/LeWM_Navigation

- For future authorized work on this project, publish a stage update here after implementation and verification: code, exact configuration/protocol, data split references, numerical results, limitations, and README updates; commit and push, then report the commit and links. Do not describe a local commit as uploaded.
- This standing instruction does not authorize new scientific experiments, extra resources, publishing unrelated data, force pushes, or changing repository visibility. Ask if the stage expands scope or requires new access.
- Every tracked directory must contain a meaningful README.md. Distinguish development results, frozen final confirmation, exploratory legacy results, and unmeasured tasks.
- Preserve original snapshots and hashes. Add a new dated phase for new work; do not silently rewrite historical frozen protocols or observations.
- Run scripts/verify_repository.py and scripts/recompute_final_table.py before publishing. Extend checks as stages are added; current scientific numerical assertions target the 2026-09-11 phase.
- Never commit secrets, SSH keys, credentials, raw datasets, checkpoints, raw videos/traces, caches, virtual environments or run logs. Keep external artifact paths/checksums and access requirements, not credential values.
- Keep the upstream LICENSE/attribution. Adapted/inspired baselines must not be called full reproductions of the named papers.
- Training seeds reuse cases; maps reuse coordinates. State conditional uncertainty, actual compute versus maximum budget, and whether latency is batch throughput or online latency.
- The original research round is complete. Do not restart historical queues/supervisors or run old queue-generation scripts merely because a snapshot contains pending-stage prose.

- Keep future publications compact: core implementation/configs, necessary reproducible result records, analysis and WM × Navigation literature review. Leave run-directory trees, queue snapshots, temporary repair scripts and duplicate reports out of the current tree. Preserve recoverability through Git history and original server archives.
