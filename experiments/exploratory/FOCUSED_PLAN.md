# 当前优先级：可信的导航局限诊断
本文件覆盖旧 PLAN.md 的待执行计划。用户已明确不要求完整复现，要求帮助选题。
总截止时间不变：2026-09-10 06:11:34（北京时间）；提前完成则停止，不为用满预算添加训练。

已执行的全部结果保留。seed3072 完整训练已完成；seed3073 当前训练继续运行，作为独立重复。
已取消：未开始的 seed3074 完整训练、PushT 从零训练。旧监督器 PID38948 已仅终止控制进程，当前训练 PID149240 保留；新监督器见 status.json 和 focus_transition.json，勿重启旧 supervisor.py。

优先执行：
1. 固定 150 个不同轨迹起点，每个同时具有 25/75 步目标，随机种子9172026；三个互不重叠的 50 例批次。没有按模型成败选择病例。
2. 不动和随机基线；公开模型、两个独立从零训练模型。short=25/50，short150=25/150，long=75/150。
3. 长目标固定病例下的搜索增量、长预测且减少候选、频繁重规划且减少迭代、频繁重规划不减迭代。报告成功率和实际批量耗时，不能声称严格等FLOPs。
4. Cube 公开权重与随机策略补齐任务背景。不再扩展新训练。
5. 按初始即成功、同侧/需过门及初始距离审计。默认物理阈值 dist<16，垂直墙 x=112，门中心y=49；源码已核对。
6. 两训练模型的表征、开环/每步真值纠正预测和固定候选成本排序审计。

已有实验提示但尚不是选题定论：
- 完整首模型短目标94.7%，长目标62.7%，原始起点集合不同，须等待新配对集再解释。
- 3k步 λ=.01 控制46%，λ=.1 控制20%，无正则24%；低有效秩不自动表示任务无用或塌缩，早期现象不代表最终收敛。
- 不得声称 TwoRooms 结果推广到未见布局、遮挡、语言导航和真实机器人。所有世界模型仍用作者同一地图数据。
- 固定候选审计仅为观察性分解，没有 oracle planning 因果介入；不能直接断言度量或动力学就是唯一瓶颈。
- 配对 bootstrap 的单位是轨迹，不跨模型坐标比较原始 latent MSE，不把三个评估批次当三个训练种子，不隐去负结果。

产物：
reports/focused_navigation_report.md（主诊断报告）
reports/focused_cases_results.csv/json（逐病例结果）
reports/paired_comparisons.json（配对差值及bootstrap区间）
reports/focused_audit.json（病例与轨迹、零动作一致性）
reports/reproduction_report.md（前期所有实验与协议差异）
focused/（预先固定病例和哈希）

监督器：focused_supervisor.py，复用 supervisor.py 的有界执行函数；每次运行后同时更新两份报告。
任何后续队列变更均需保留截止时间和已运行检查点，不启动重复 GPU 进程。
